// Tables de référence (listes de valeurs)

model TypePlante {
  id      Int      @id @default(autoincrement())
  libelle String   @unique
  plantes Plante[]
}

model TypeSol {
  id        Int        @id @default(autoincrement())
  libelle   String     @unique
  parcelles Parcelle[]
}

model StatutCulture {
  id       Int       @id @default(autoincrement())
  libelle  String    @unique
  cultures Culture[]
}

model CategorieStock {
  id         Int         @id @default(autoincrement())
  libelle    String      @unique
  stockItems StockItem[]
}
